export const enum EngineSettings {
  canvasWidth = 1920,
  canvasHeight = 400,
}

export const enum EngineStatus {
  Running,
  Unmounted,
}

export enum ActualScreen {
  startScreen,
  gameScreen,
  overScreen,
}
